# 2026-07-29

## 二游日历 APP 需求确认 + 风格稿

- 项目：Flutter 二游日历（acg_calendar），Android 单用户使用，不对外发布
- 已确认需求：APP 内直接抓取官方源（无后端）；游戏=米哈游系/鹰角/库洛/Nikke国服/阴阳师/异环，设置页勾选筛选；首屏=日历+当日列表结合；不要推送要缓存（Hive：公告 6h、卡池 12h）；详情页每游戏沉浸式独立主题，卡池 UP 角色立绘（兜底看板娘）+公告/卡池/活动/原文跳转
- 用户不懂架构，技术决策全权委托；Flutter 环境已配好
- 产出：docs/plans/2026-07-29-acg-game-calendar-design.md；三套首页 HTML 风格稿（design-drafts/：A 明亮幻想 / B 深色科幻 / C 清新简约），等用户选风格

## 风格定稿 + Flutter 实现（同日）
- 用户拍板：**日间=A 明亮幻想，夜间=B 深色科幻，加日夜切换按钮**，C 弃用
- 数据源实测：米游社 getNewsList 已 404 下线；**getForumPostList 可用**（forum_id：原神28/崩铁53/绝区零58/崩3-6），按标题关键词分类版本/卡池/活动；方舟/鸣潮/战双/Nikke/阴阳师走官网 HTML 抓取+回退
- 环境：Flutter 3.24.5 / Dart 3.5.4 / Java 17，flutter 在 `C:\flutter\bin\flutter.bat`（不在 PATH）
- 踩坑：① flutter 启动锁 = 残留 dart 进程，杀进程+删 `C:\flutter\bin\cache\lockfile`；② `pub get` 报 Null check（advisories 缓存损坏）= 删 `Pub\Cache\hosted\pub.flutter-io.cn\.cache`
- 已实现完整 APP：主题/模型/Adapter/缓存/首页(日历+列表)/详情页(沉浸主题+3Tab)/设置页(筛选)，`flutter analyze` 0 issues

## APK v1.0.0 + 数据源修复（同日）
- 构建踩坑：AGP 8.1 + jbr 路径含空格导致 JdkImageTransform 失败 → 升级 AGP 8.3.2 + Gradle 8.4 解决
- 用户反馈"所有游戏都没数据"：`dart run tool/fetch_test.dart` 复现——米哈游系 4 款 + 阴阳师正常；方舟/鸣潮/战双抓空（JS 动态渲染）；Nikke URL 404
- 修复：① 方舟专用解析器（SSR 页内嵌 JSON：cid/title/displayTime/brief 正则，30 条 ✅）；② 鸣潮/战双/Nikke 需逆向，暂"数据源校准中"占位；③ **数据源自检**：repository 记录每源 ok:n/empty/error:msg 到 prefs，设置页逐游戏显示 ✅/⚠️/❌，下拉刷新出汇总 SnackBar
- 调试工具：`tool/fetch_test.dart`（flutter SDK 内 dart 直接跑，脱离设备验证数据源）
- APK：`build/app/outputs/flutter-apk/app-release.apk`（21.5MB）
- 待办：用户真机看设置页自检——若米游社也 error 则是设备网络问题；后续校准鸣潮/战双/Nikke 源；卡池角色立绘精确提取

## v1.1 八项反馈修复（同日）
1. **首页按游戏分组+折叠**：GroupedDayEvents，组头图标/名称/条数，点击展开收起
2. **公告点不开根因**：Android 11+ 包可见性，`canLaunchUrl` 无 https queries 恒 false → manifest 加 VIEW https/http queries；点击直开原文（openLink），无链接回退详情页
3. **异环已上线**：摘占位，YihuanSource 解析 yh.wanmei.com/m/news/（SSR：h2.title/.des/.date），服务端只给 4 条最新
4. **鸣潮/战双数据源破解**：库洛官网 OSS 静态 JSON——`media-cdn-mingchao.kurogame.com/akiwebsite/website2.0/json/G152/zh/ArticleMenu.json`（鸣潮）、`media-cdn-zspms.kurogame.com/pnswebsite/website2.0/json/G144/ArticleMenu.json`（战双），KuroCmsSource 通用解析各 30 条 ✅
5. Nikke 仍占位：官网腾讯 milo 网关多级 token（act_id/api_cfg flows），逆向成本高留待下轮
6. **APP 图标**：Game.iconUrl + CachedNetworkImage（米系=米游社 getGameList、战双=官网 favicon.png、Nikke=gtimg AppIcon、阴阳师=netease icon、异环=NTE_logo；方舟/鸣潮只有 .ico Flutter 不支持，保留汉字块）
7. **详情页头图**：该游戏最新带封面事件图（版本/卡池宣传图）+ 主题色渐变罩层，兜底 emoji
8. 数据源战绩：米系4+方舟+鸣潮+战双+阴阳师+异环 = **9/10 可用**
- 经验：腾讯系站点需带 Referer（nikke.qq.com 无 Referer 返回伪 404 模板）；库洛官网新闻=OSS 静态 JSON（翻 JS bundle 里 VITE_APP_JSON_PATH_ZH）

## v1.2 第二轮七项反馈修复（同日）
1. **Nikke 数据源**：改用 gamekee Wiki API `https://www.gamekee.com/v1/content/list?type=2&page=1&limit=20` + header `game-alias: nikke`，type=2=官方资讯，20 条 ✅。**10/10 数据源全部可用**
2. **米游社公告链接 404**：URL 格式应为 `https://www.miyoushe.com/{slug}/article/{id}`，slug：原神=ys/崩铁=sr/绝区零=zzz/崩3=bh3（之前用了不存在的 /article/{id}）。新增 Game.miyousheSlug 字段
3. **分组头部点击进详情页**：整行 tap→GameDetailPage，展开/收起改为右侧独立箭头按钮
4. **阴阳师日期错放今天**：根因是旧 extractDate 只认"M月D日"，而阴阳师标题前缀是"07-25"格式→回退 DateTime.now()。扩展 extractDate 增加"MM-DD "匹配
5. **图标/头图换源**：鸣潮图标改用 TapTap 应用图标（img.tapimg.com，PNG 可解码）；方舟仍无 PNG 图标源保留汉字块。头图：方舟加 pcCover 提取（轮播项）、库洛系 articleCover+内容首图、异环列表 img、米游社 images+内容首图；详情页无封面时兜底用游戏图标放大显示
6. **卡池分类合并**：详情页去掉"公告/卡池/活动"三 Tab，改为单条时间线倒序（最新在上），用类型标签区分——避免卡池分类不准/空 Tab 问题
7. **方舟"老公告"**：实际 SSR 最新到 07-25（夏日嘉年华），8.1 版本公告尚未发布；详情页改倒序后最新在最上，不再误判为老数据
- 经验2：gamekee API 需 `game-alias` 请求头；Dart 原始字符串 r'...' 不能含未转义的同引号，用 r'''...''' 三引号规避

## v1.3 六项错误修复（同日，未提交，仅出 APK）
1. **内置浏览器**：新增 `webview_flutter: ^4.10.0` + `lib/ui/webview_page.dart`（前进/后退/刷新/用浏览器打开）；`openLink` 改为 `openLink(BuildContext, url)` 推入 WebView，不再跳外部浏览器。调用点 detail_page / home_page 同步加 context。
2. **库洛(鸣潮/战双)标签+乱码**：根因 `articleContent` 是多行 HTML，旧正则 `<[^>]*>` 跨换行失效→残留 `<p data-eva3-s` 等标签与实体乱码。改用 `html` 包 `parse().body.text` 的 `cleanText()` 统一清洗（去标签/解码实体/还原 `//n //t //r` 二次转义换行/折叠空白）。同理应用到米游社/方舟/异环/gamekee/OfficialWeb。
3. **鸣潮/异环球图**：
   - 异环：列表页无图，改抓前 6 条详情页（`/m/news/...html`）取 `resources/jpg` banner 作为 cover（并行 Future.wait）。
   - 战双：cover 来自 `articleCover` 字段（已验证有值）。
   - **鸣潮无头图源**：Kuro G152 JSON 全量 `articleCover`/`suggestCover` 均为空，正文亦无 `<img src>`（JS 懒加载，详情页仅 8.6KB 纯 JS 壳）。无可用 banner → 详情头图兜底为 TapTap 图标 + 游戏名（已把无 cover 的 header 改为主题化大图标+名称，观感更完整）。已向用户说明此限制。
4. **方舟图标 + //n**：iconUrl 改用官方品牌 PNG `web.hycdn.cn/arknights/official/_next/static/media/amiya.a410de75.png`（200 image/png，直连可解码）；brief/title 经 `cleanText()` 还原 `//n` 等转义。
5. **Nikke 链接 404→首页**：根因 detailBase 少了 `.html` 后缀。gamekee 真实文章 URL 是 `https://www.gamekee.com/nikke/{id}.html`（已验证 200 无重定向；多例真实链接如 170671.html）。改为 `{id}.html`。
6. **方舟公告 //n//n**：同上 `cleanText()` 处理（`//n`→空格）。
- 构建：`flutter build apk --release` → `build/app/outputs/flutter-apk/app-release.apk`（未 git 提交，用户要求先不提交）。
- 调试经验：curl 在本机 OK；tapimg/taptap 在本沙箱被拦截返回空（设备端可直连）；gamekee/nikke 列表为 SPA，URL 规律靠 web 搜索确认而非抓 HTML。
